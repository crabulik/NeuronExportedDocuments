@using NeuronExportedDocuments.Resources
@{
    ViewBag.Title = IdentityResources.rs_ConfirmAccount;
}

@section Scripts {

}
@section Body {
    <h2>@ViewBag.Title.</h2>
    <div>
        <p>
            @Html.Raw(string.Format(IdentityResources.rs_ConfirmationThanks, Html.Raw(Html.ActionLink(IdentityResources.rs_ClickHereToLogIn, "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" }))))
        </p>
    </div>
}